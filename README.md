# Laravel with TailwindCSS

Default [Laravel 8](https://laravel.com/docs/8.x) installation with [TailwindCSS](https://tailwindcss.com/) included and configured.

### Install

```bash
composer install \
&& npm install && npm run dev
```